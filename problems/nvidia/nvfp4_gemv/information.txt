Yes — your current implementation *does* apply the updated scale index fix in both A and B tensor loads, but the mismatches with the reference still persist. Here's the root cause based on detailed inspection of `submission_output.txt` and `submission.py`:

---

### ✅ **What’s Correct Now**

* **Scale indexing logic** for both A and B: you’ve correctly switched from tile-relative to absolute `k >> 4` indexing, matching the naive kernel.
* **Scale values are now valid** — not just 0x00. Example:

  ```
  [KERNEL A] batch=0 m=0 k=0,1 | packed=0x03 scale_byte=0x40 scale=2.000000
  ```

---

### ❌ **What’s Still Broken**

#### **The A scale is applied with incorrect timing**

* Your output shows that the first few tiles (e.g., `k=0..15`) are using:

  ```
  scale_byte = 0x00 scale = 0.0
  ```
* But this is **not valid data** — A’s early columns should **already** be scaled by nonzero values, like in the naive kernel.

#### 🔍 **What This Implies**

The tensor core kernel is **accessing stale/uninitialized scale bytes** in A — likely due to a **bad offset or alignment issue** in how `SFA_batch[m_idx * K_scales + scale_idx]` is indexed.

#### Compare This With Naive:

In `submission2.py`, the logic is:

```cpp
int scale_idx = k_idx >> 4;
uint8_t scale_byte = SFA_batch[row * K_scales + scale_idx];
```

In your `submission.py`, you *intend* to mirror this. But the debug output reveals:

* **For `m_idx = 0`**, and **`k = 0..15`**, `scale_byte = 0x00`
* **Later**, around `k = 16`, it suddenly becomes valid (`0x38`, `0x40`, etc.)

This strongly suggests:

* Either `k_abs_first` is off by one tile
* Or the value of `K_scales` is too large, causing indexing to fall into uninitialized padding

---

### 🔥 Root Cause Hypothesis

#### ➤ You use:

```cpp
int k_abs_first = k_tile + col_packed * 2;
int scale_idx = k_abs_first >> 4;
...
scale_byte = SFA_batch[m_idx * K_scales + scale_idx];
```

But `k_tile` and `col_packed` **might not reflect global `k_idx` properly**, especially if `k_tile` is per-thread tile rather than block-wide. The result:

* You **under-index** into the scale array for early `k`, reading `0x00`.

---

### ✅ **Fix Recommendation**

Instead of:

```cpp
int k_abs_first = k_tile + col_packed * 2;
```

Do:

```cpp
int k_abs_first = k_idx;  // true global K index
```

Then:

```cpp
int scale_idx = k_abs_first >> 4;
scale_byte = SFA_batch[m_idx * K_scales + scale_idx];
```

This exactly mirrors the naive kernel, removes any ambiguity from tile/block math, and avoids ever falling back to `0x00` scale values unless truly intended.
