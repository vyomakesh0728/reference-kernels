Here's **precisely what's wrong** in submission.py, not generalized—based on your own debug output and direct code logic differences:

***

### **The Failure Is in FP8 Block Scale Factor Handling:**
#### **Your tensor core kernel (submission.py) uses scale = 0.0 for most elements.**

- In `[KERNEL A]` and `[KERNEL B]` debug outputs, after a certain K, all scale_bytes and decoded scales become 0, so all scaled values become zero (except some very initial input).
- The **naive kernel (submission2.py)** always has correct scale decode, multiplying FP4 decode by the correct FP8 scale (both for A and B), for every input element.

***

### **Pinpointed Error: How You Index FP8 Block Scales in the PTX Kernel Setup**
1. **Your scale array index math** for shared memory decode appears to go out of bounds, or overwrites scale_bytes with zero, especially for batches $$ m \neq 0 $$ and all $$ k \geq 32 $$.
2. In the tiling loop:
   - You set up `scaleglobalidx = kscaletile + scaleidx` for A and B.
   - For all tile sizes after the first, `scaleglobalidx` ends up beyond SFApacked/SFBpacked’s valid length, so any `if scaleglobalidx < Kscales` failure results in scale=1.0 or scale=0.0.
3. If you PRINT `scaleglobalidx` in your kernel, you'll see only the first tile can read valid scales. You need to wrap/recompute per-row and per-batch stride math.

#### **Compare to submission2.py (Naive kernel):**
- Indexes `scaleidx = kidx // 16` in a single pass, correctly per-element, never exceeding bounds.
- Always gets scale factor for the corresponding `kidx`, every element.

***

### **Output Consequences**
Because almost all elements are decoded/scaled with `scale=0.0`, the output is zero or random, except for the first tile, first batch, and first column/block.

- **First error example:**
  ```
  ERROR AT (0, 0, 0): 0.0 4668.0
  ```
  indicates kernel output `0.0` (all scale 0) vs reference `4668.0` (correct decode and scale sum).

***

### **What To Fix:**
- In shared memory decode (tile loader) of your PTX kernel, correct the calculation and application of fp8 block scale:
    - **For every row and every block, confirm the shape and stride math matches both kernel and memory layout.**
    - **NEVER let scaleglobalidx exceed scale array size.**

##### **Quick check/fix plan:**
- **Add:**
  ```cpp
  if (scaleglobalidx >= Kscales) printf("SCALE IDX OUT OF BOUNDS: %d\n", scaleglobalidx);
  ```
- **Fix:**
  Instead of adding fixed offsets, compute scale index directly per row and k-block like you do in the naive kernel.

### **In summary:**
> Your shared memory decode and scale math in the tensor-core kernel **does not correctly load the FP8 block scale**, causing your output to be zeroed except for initial elements. Your reference/naive kernel never fails this block scale lookup.
> **Fix the scale index math in Phase 1/2, and the output will immediately match the reference**.
